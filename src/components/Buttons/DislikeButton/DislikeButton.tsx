/* eslint-disable jsx-a11y/control-has-associated-label */

import React from 'react';
import classes from './DislikeButton.module.scss';

const DislikeButton = () => {
  return (
    <button
      type="button"
      className={classes.button}
    />
  );
};

export default DislikeButton;
